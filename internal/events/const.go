package events

const (
	// EventCreate identifier for creation events.
	EventCreate = "Create"
	// EventDelete identifier for deletion events.
	EventDelete = "Delete"
	// EventUpdate identifier for update events.
	EventUpdate = "Update"
	// EventError identifier for error events.
	EventError = "Error"
)
