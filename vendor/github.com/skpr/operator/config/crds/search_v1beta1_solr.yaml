apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  labels:
    controller-tools.k8s.io: "1.0"
  name: solrs.search.skpr.io
spec:
  group: search.skpr.io
  names:
    kind: Solr
    plural: solrs
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          properties:
            coreName:
              description: Name of the core which will be provisioned.
              type: string
            resources:
              description: Resources given to the Solr instance.
              properties:
                cpu:
                  description: CPU to use when provisioning Solr.
                  properties:
                    limit:
                      description: CPU limits given to the Solr process.
                      type: object
                    request:
                      description: CPU requests given to the Solr process.
                      type: object
                  required:
                  - request
                  - limit
                  type: object
                memory:
                  description: Memory for the Solr instance. We don't set requests
                    and limits because memory is important with Java applications.
                  type: object
                storage:
                  description: Storage which is mounted for Solr.
                  type: object
              required:
              - cpu
              - memory
              - storage
              type: object
            version:
              description: Version refers to the version/tag to use on the solr image
                repository.
              type: string
          required:
          - coreName
          - version
          - resources
          type: object
        status:
          properties:
            core:
              description: Core which applications will use for indexing.
              type: string
            host:
              description: Host which applications will use to interact with Solr.
              type: string
            port:
              description: Port which applications will use to interact with Solr.
              format: int64
              type: integer
          required:
          - host
          - port
          - core
          type: object
  version: v1beta1
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
