apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  labels:
    controller-tools.k8s.io: "1.0"
  name: imagescheduleds.mysql.skpr.io
spec:
  group: mysql.skpr.io
  names:
    kind: ImageScheduled
    plural: imagescheduleds
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          properties:
            schedule:
              properties:
                concurrencyPolicy:
                  description: 'Specifies how to treat concurrent executions of a
                    Job. Valid values are: - "Allow" (default): allows CronJobs to
                    run concurrently; - "Forbid": forbids concurrent runs, skipping
                    next run if previous run hasn''t finished yet; - "Replace": cancels
                    currently running job and replaces it with a new one'
                  type: string
                cronTab:
                  description: The schedule in Cron format, see https://en.wikipedia.org/wiki/Cron.
                  type: string
                failedJobsHistoryLimit:
                  description: The number of failed finished jobs to retain. This
                    is a pointer to distinguish between explicit zero and not specified.
                  format: int32
                  type: integer
                startingDeadlineSeconds:
                  description: Optional deadline in seconds for starting the job if
                    it misses scheduled time for any reason.  Missed jobs executions
                    will be counted as failed ones.
                  format: int64
                  type: integer
                successfulJobsHistoryLimit:
                  description: The number of successful finished jobs to retain. This
                    is a pointer to distinguish between explicit zero and not specified.
                  format: int32
                  type: integer
                suspend:
                  description: This flag tells the controller to suspend subsequent
                    executions, it does not apply to already started executions.  Defaults
                    to false.
                  type: boolean
              required:
              - cronTab
              type: object
            template:
              description: Template which will be used to trigger an image build.
              properties:
                connection:
                  description: Connection details for the mysql image.
                  properties:
                    configmap:
                      description: ConfigMap which will be used for connectivity.
                      properties:
                        keys:
                          description: ConfigMap keys used for connectivity.
                          properties:
                            database:
                              description: Database which was applied to the application
                                for database connectivity.
                              type: string
                            hostname:
                              description: Hostname which was applied to the application
                                for database connectivity.
                              type: string
                            port:
                              description: Port which was applied to the application
                                for database connectivity.
                              type: string
                          required:
                          - database
                          - hostname
                          - port
                          type: object
                        name:
                          description: Name of the ConfigMap.
                          type: string
                      required:
                      - name
                      - keys
                      type: object
                    secret:
                      description: Secret which will be used for connectivity.
                      properties:
                        keys:
                          description: Secret keys used for connectivity.
                          properties:
                            password:
                              description: Password which was applied to the application
                                for database connectivity.
                              type: string
                            username:
                              description: Username which was applied to the application
                                for database connectivity.
                              type: string
                          required:
                          - username
                          - password
                          type: object
                        name:
                          description: Name of the Secret.
                          type: string
                      required:
                      - name
                      - keys
                      type: object
                  required:
                  - configmap
                  - secret
                  type: object
                destinations:
                  description: Destinations to push the  mysql image to.
                  items:
                    type: string
                  type: array
                rules:
                  description: Rules for the mysql image.
                  properties:
                    ignore:
                      items:
                        type: string
                      type: array
                    nodata:
                      items:
                        type: string
                      type: array
                    rewrite:
                      type: object
                  required:
                  - rewrite
                  - nodata
                  - ignore
                  type: object
              required:
              - connection
              - rules
              - destinations
              type: object
          required:
          - schedule
          - template
          type: object
        status:
          properties:
            active:
              description: A list of pointers to currently running jobs.
              items:
                type: object
              type: array
            lastScheduleTime:
              description: Information when was the last time the job was successfully
                scheduled.
              format: date-time
              type: string
          type: object
  version: v1beta1
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
