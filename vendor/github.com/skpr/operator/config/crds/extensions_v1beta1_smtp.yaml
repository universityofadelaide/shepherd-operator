apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  labels:
    controller-tools.k8s.io: "1.0"
  name: smtps.extensions.skpr.io
spec:
  group: extensions.skpr.io
  names:
    kind: SMTP
    plural: smtps
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          properties:
            from:
              description: From defines what an application is allowed to send from.
              properties:
                address:
                  description: Address which an application is allowed to send from.
                  type: string
              required:
              - address
              type: object
          required:
          - from
          type: object
        status:
          properties:
            connection:
              description: Provides connection details for sending email.
              properties:
                hostname:
                  description: Hostname used when connecting to the SMTP server.
                  type: string
                password:
                  description: Password used when connecting to the SMTP server.
                  type: string
                port:
                  description: Port used when connecting to the SMTP server.
                  format: int64
                  type: integer
                region:
                  description: Region which the SMTP server resides.
                  type: string
                username:
                  description: Username used when connecting to the SMTP server.
                  type: string
              type: object
            verification:
              description: Provides the status of verifying FROM attributes.
              properties:
                address:
                  description: Address which an application is allowed to send from.
                  type: string
              type: object
          type: object
  version: v1beta1
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
