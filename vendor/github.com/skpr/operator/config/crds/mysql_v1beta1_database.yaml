apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  labels:
    controller-tools.k8s.io: "1.0"
  name: databases.mysql.skpr.io
spec:
  group: mysql.skpr.io
  names:
    kind: Database
    plural: databases
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          properties:
            privileges:
              description: Privileges which the application requires.
              items:
                type: string
              type: array
            provisioner:
              description: Provisioner used to create databases.
              type: string
          required:
          - provisioner
          - privileges
          type: object
        status:
          properties:
            connection:
              description: Connection details for the database.
              properties:
                ca:
                  description: CA used when connecting to the database.
                  type: string
                database:
                  description: Database used when connecting to the database.
                  type: string
                hostname:
                  description: Hostname used when connecting to the database.
                  type: string
                password:
                  description: Password used when connecting to the database.
                  type: string
                port:
                  description: Port used when connecting to the database.
                  format: int64
                  type: integer
                username:
                  description: Username used when connecting to the database.
                  type: string
              type: object
            observedGeneration:
              description: Used for determining if an APIs information is up to date.
              format: int64
              type: integer
            phase:
              description: Current state of the database being provisioned.
              type: string
          type: object
  version: v1beta1
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
