apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  labels:
    controller-tools.k8s.io: "1.0"
  name: certificates.aws.skpr.io
spec:
  group: aws.skpr.io
  names:
    kind: Certificate
    plural: certificates
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          properties:
            request:
              description: Information which will be used to provision a certificate.
              properties:
                alternateNames:
                  description: Additional domains for the certificate request.
                  items:
                    type: string
                  type: array
                commonName:
                  description: Primary domain for the certificate request.
                  type: string
              required:
              - commonName
              type: object
          required:
          - request
          type: object
        status:
          properties:
            active:
              description: The status of the most recently ISSUED certificate.
              properties:
                details:
                  description: Details of the certificate.
                  properties:
                    arn:
                      description: Machine identifier for the certificate request.
                      type: string
                    domains:
                      description: Domain list for the certificate.
                      items:
                        type: string
                      type: array
                    observedGeneration:
                      description: Used for determining if an APIs information is
                        up to date.
                      format: int64
                      type: integer
                    state:
                      description: Current state of the certificate eg. ISSUED.
                      type: string
                    validate:
                      description: Details used to validate a certificate request.
                      items:
                        properties:
                          name:
                            description: The name of DNS validation record.
                            type: string
                          status:
                            description: The status of the DNS validation record.
                            type: string
                          type:
                            description: The type of DNS validation record.
                            type: string
                          value:
                            description: The value of DNS validation record.
                            type: string
                        type: object
                      type: array
                  type: object
                name:
                  description: Reference name for the certificate request.
                  type: string
              type: object
            desired:
              description: The status of the desired certificate.
              properties:
                details:
                  description: Details of the certificate.
                  properties:
                    arn:
                      description: Machine identifier for the certificate request.
                      type: string
                    domains:
                      description: Domain list for the certificate.
                      items:
                        type: string
                      type: array
                    observedGeneration:
                      description: Used for determining if an APIs information is
                        up to date.
                      format: int64
                      type: integer
                    state:
                      description: Current state of the certificate eg. ISSUED.
                      type: string
                    validate:
                      description: Details used to validate a certificate request.
                      items:
                        properties:
                          name:
                            description: The name of DNS validation record.
                            type: string
                          status:
                            description: The status of the DNS validation record.
                            type: string
                          type:
                            description: The type of DNS validation record.
                            type: string
                          value:
                            description: The value of DNS validation record.
                            type: string
                        type: object
                      type: array
                  type: object
                name:
                  description: Reference name for the certificate request.
                  type: string
              type: object
            observedGeneration:
              description: Used for determining if an APIs information is up to date.
              format: int64
              type: integer
            requests:
              description: Status of all the certificate requests.
              items:
                properties:
                  details:
                    description: Details of the certificate.
                    properties:
                      arn:
                        description: Machine identifier for the certificate request.
                        type: string
                      domains:
                        description: Domain list for the certificate.
                        items:
                          type: string
                        type: array
                      observedGeneration:
                        description: Used for determining if an APIs information is
                          up to date.
                        format: int64
                        type: integer
                      state:
                        description: Current state of the certificate eg. ISSUED.
                        type: string
                      validate:
                        description: Details used to validate a certificate request.
                        items:
                          properties:
                            name:
                              description: The name of DNS validation record.
                              type: string
                            status:
                              description: The status of the DNS validation record.
                              type: string
                            type:
                              description: The type of DNS validation record.
                              type: string
                            value:
                              description: The value of DNS validation record.
                              type: string
                          type: object
                        type: array
                    type: object
                  name:
                    description: Reference name for the certificate request.
                    type: string
                type: object
              type: array
          type: object
  version: v1beta1
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
