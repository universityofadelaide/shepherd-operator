apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  labels:
    controller-tools.k8s.io: "1.0"
  name: restores.extensions.skpr.io
spec:
  group: extensions.skpr.io
  names:
    kind: Restore
    plural: restores
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          properties:
            backupName:
              description: BackupName to restore from.
              type: string
            mysql:
              description: MySQL databases which will be restored to.
              type: object
            secret:
              description: Secret which defines information about the restic secret.
              properties:
                key:
                  description: Key of the secret data to use for the password
                  type: string
                name:
                  description: Name of the secret.
                  type: string
              required:
              - name
              - key
              type: object
            volumes:
              description: Volumes which will be restored to.
              type: object
          required:
          - backupName
          - secret
          type: object
        status:
          properties:
            completionTime:
              description: The time the restore completed.
              format: date-time
              type: string
            phase:
              description: The phase the restore is in.
              type: string
            startTime:
              description: The time the restore started.
              format: date-time
              type: string
          required:
          - phase
          type: object
  version: v1beta1
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
